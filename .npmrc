# Preserve peer dependencies to prevent aggressive deduplication
legacy-peer-deps=false
strict-peer-deps=false

# Use default hoisting strategy but preserve nested optional peer deps
install-strategy=hoisted