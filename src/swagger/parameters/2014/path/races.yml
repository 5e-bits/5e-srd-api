name: index
in: path
required: true
description: |
  The `index` of the race to get.
schema:
  type: string
  enum:
    - dragonborn
    - dwarf
    - elf
    - gnome
    - half-elf
    - half-orc
    - halfling
    - human
    - tiefling
  example: elf
