get:
  summary: "Get a weapon property by index."
  tags:
    - Equipment
  parameters:
    - $ref: "../parameters/path/indexParam.yml"
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            $ref: "../schemas/combined.yml#/WeaponProperty"
