get:
  summary: "Get a rule section by index."
  tags:
    - Rules
  parameters:
    - $ref: "../parameters/path/indexParam.yml"
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            $ref: "../schemas/combined.yml#/RuleSection"
