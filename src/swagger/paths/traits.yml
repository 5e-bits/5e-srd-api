get:
  summary: "Get a trait by index."
  tags:
    - Traits
  parameters:
    - $ref: "../parameters/path/indexParam.yml"
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            $ref: "../schemas/combined.yml#/Trait"
