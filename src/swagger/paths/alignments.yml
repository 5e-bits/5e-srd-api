get:
  summary: "Get an alignment by index."
  tags:
    - Character Data
  parameters:
    - $ref: "../parameters/path/indexParam.yml"
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            $ref: "../schemas/combined.yml#/Alignments"
