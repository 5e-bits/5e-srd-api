get:
  summary: "Get a condition by index."
  tags:
    - Game Mechanics
  parameters:
    - $ref: "../parameters/path/indexParam.yml"
  responses:
    "200":
      description: "OK"
      content:
        application/json:
          schema:
            $ref: "../schemas/combined.yml#/Condition"
