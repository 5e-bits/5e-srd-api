name: index
in: path
required: true
description: |
  The `index` of the rule to get.

  Available values can be found in the [`ResourceList`](#get-/api/2014/-endpoint-) for `rules`.
schema:
  type: string
  example: ability-checks
