language: node_js
node_js:
  - 12

branches:
  only:
    - master

jobs:
  include:
    - stage: Test
      script: npm run lint
    - # stage name not required, will continue to use `Test`
      script: npm test
